## 未來如何避免

1. Component 改依賴來源時，對應測試的 mock 必須同步更新。
2. UI wrapper / unit test 應避免載入 store、service、worker、WASM、OpenCV 等 heavyweight dependency。
3. Store 檔案應避免 top-level import heavy module；若只在特定 function 內使用，建議改成 lazy import。
4. 若 Vitest 卡在 `[queued]` 或只顯示 `RUN`，優先懷疑 module import / test collection，而不是 assertion 或 async test body。

這次就是透過此方式確認卡點是 `@techstark/opencv-js` 的 import。